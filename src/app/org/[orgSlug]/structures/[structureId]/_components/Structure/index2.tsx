"use client"

import { UserIdentity } from "convex/server"

interface Props {
  currentStructureUser: UserIdentity
}

export default function Structure2({ currentStructureUser }: Props) {
  // console.log(currentStructureUser)
  // const convexNodes = useQuery(api.nodes.getByStructureId, {
  //   structureId: structure.id,
  // })

  // console.log(convexNodes)

  return null
}
